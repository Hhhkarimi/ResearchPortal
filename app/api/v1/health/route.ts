import {NextResponse} from "next/server";
import {audits,datasetSummary,deepAudits,dimensionEvidence,institutions,researchReviews} from "@/lib/data";
export const dynamic="force-static";
export function GET(){const healthy=institutions.length===115&&audits.length===115&&deepAudits.length===115&&researchReviews.length===115&&dimensionEvidence.length===920;return NextResponse.json({status:healthy?"ok":"degraded",checks:{iscRoster:institutions.length,portalAudits:audits.length,deepAuditRows:deepAudits.length,researchReviews:researchReviews.length,dimensionEvidenceOutcomes:dimensionEvidence.length,referentialIntegrity:healthy},snapshotDate:datasetSummary.snapshotDate,methodologyVersion:datasetSummary.methodologyVersion},{status:healthy?200:503,headers:{"Access-Control-Allow-Origin":"*","Cache-Control":"no-store"}})}
