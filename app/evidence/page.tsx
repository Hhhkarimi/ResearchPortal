import {provenanceLedger} from "@/lib/data";export const metadata={title:"Evidence Ledger"};export default function Page(){return <main className="shell page"><header className="pageHero"><span className="kicker">PROVENANCE / EVIDENCE LEDGER</span><h1>دفتر شواهد</h1><p>هر Fact قابل انتشار باید به URL منبع و تاریخ verification متصل باشد. این صفحه ادعاها را به منابعشان وصل می‌کند.</p></header><div className="evidenceTable glass">{provenanceLedger.map((x:any)=><a href={x.sourceUrl} target="_blank" rel="noopener noreferrer" className="evidenceRow" key={x.id}><span>{x.entityType}</span><b>{x.claim}</b><i>{x.universitySlug}</i><em>{x.evidenceLevel}</em><small>↗</small></a>)}</div></main>}
