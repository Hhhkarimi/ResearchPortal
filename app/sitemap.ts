import type {MetadataRoute} from "next";import {institutions} from "@/lib/data";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://example.com";return["","/isc-scope","/universities","/rankings","/compare","/documents","/audit","/evidence","/datasets","/api-docs","/methodology",...institutions.map(x=>`/universities/${x.slug}`)].map(url=>({url:base+url,lastModified:new Date("2026-08-10")}))}
