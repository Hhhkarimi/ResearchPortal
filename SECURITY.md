# Security

- CSP / X-Frame / no-sniff / referrer / permissions headers
- read-heavy app; no public write endpoint
- crawler uses HTTPS, timeout and rate delay
- no raw crawled HTML is rendered
- GitHub CodeQL + Dependabot
- never commit secrets
