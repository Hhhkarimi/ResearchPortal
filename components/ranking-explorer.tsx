"use client";

import Link from "next/link";
import {useDeferredValue,useMemo,useState} from "react";

export function RankingExplorer({rankings}:{rankings:any[]}){const [category,setCategory]=useState("همه");const [query,setQuery]=useState("");const deferredQuery=useDeferredValue(query);const categories=useMemo(()=>[...new Set(rankings.map(x=>x.iscCategory))],[rankings]);const rows=useMemo(()=>rankings.filter(r=>(category==="همه"||r.iscCategory===category)&&(!deferredQuery||r.nameFa.includes(deferredQuery))),[rankings,category,deferredQuery]);return <><div className="explorerBar compact glass"><div className="field searchField"><label htmlFor="ranking-search">جست‌وجو</label><input id="ranking-search" type="search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="نام دانشگاه…" spellCheck={false}/></div><div className="field"><label htmlFor="ranking-category">گروه ISC</label><select id="ranking-category" value={category} onChange={e=>setCategory(e.target.value)}><option>همه</option>{categories.map(x=><option key={x}>{x}</option>)}</select></div></div><div className="rankTable"><div className="rankHead"><span>رتبه</span><span>پرتال معاونت پژوهشی و فناوری</span><span>در گروه ISC</span><span>پوشش / اطمینان</span><span>RTPMI</span></div>{rows.map((r:any)=><Link href={`/universities/${r.universitySlug}`} className="rankRow" key={r.universitySlug}><b>{r.rank.toLocaleString("fa-IR")}</b><span><strong>{r.nameFa}</strong><small>{r.iscCategory} · رتبه ISC {r.iscRank.toLocaleString("fa-IR")}</small></span><i>{r.portalRankWithinISCClass.toLocaleString("fa-IR")} از {r.rankedPortalsInISCClass.toLocaleString("fa-IR")}</i><div className="confidence"><span><i style={{width:`${r.confidence}%`}}/></span><small>{r.evidenceCoverage}% پوشش · {r.confidence}% اطمینان</small></div><strong>{r.score}</strong></Link>)}</div></>}
