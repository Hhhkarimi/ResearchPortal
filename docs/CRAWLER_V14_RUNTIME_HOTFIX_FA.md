# Crawler v14 self-hosted runtime hotfix

این Hotfix مرحله actions/setup-node را حذف می‌کند تا workflow از Node نصب‌شده روی Windows self-hosted runner استفاده کند. Checkpoint/Resume سی‌دقیقه‌ای و تنظیمات Crawl v14 تغییری نکرده‌اند.
