diff --git a/app/compare/page.tsx b/app/compare/page.tsx
index 68c48a6..cfe2a2d 100644
--- a/app/compare/page.tsx
+++ b/app/compare/page.tsx
@@ -8,7 +8,7 @@ import {
 
 export const metadata = {
   title:
-    "مقایسه تعاملی پرتال‌های پژوهش و فناوری"
+    "مقایسه تعاملی پرتال‌های معاونت پژوهشی و فناوری"
 };
 
 export default function Page() {
@@ -25,8 +25,8 @@ export default function Page() {
           </h1>
 
           <p>
-            دو تا چهار پرتال Evidence-qualified را انتخاب کنید و
-            هشت مؤلفه امتیازدهی بلوغ پرتال را کنار هم ببینید.
+            دو تا چهار پرتال معاونت پژوهشی و فناوریِ Evidence-qualified را انتخاب کنید و
+            هشت مؤلفه امتیازدهی بلوغ پرتال معاونت پژوهشی و فناوری را کنار هم ببینید.
           </p>
         </div>
 
diff --git a/app/datasets/page.tsx b/app/datasets/page.tsx
index 265d96a..77f3649 100644
--- a/app/datasets/page.tsx
+++ b/app/datasets/page.tsx
@@ -5,7 +5,7 @@ export const metadata={title:"Open Data و API | ISC ۱۱۵"};
 
 const sets=[
   ["فهرست ۱۱۵ نهاد ISC","JSON + CSV","isc-institutions"],
-  ["ممیزی پرتال ۱۱۵ نهاد","JSON + CSV","audit-portal-audit"],
+  ["ممیزی پرتال معاونت پژوهشی و فناوری ۱۱۵ نهاد","JSON + CSV","audit-portal-audit"],
   ["رجیستر ۸۰۵ outcome شواهد","JSON + CSV","evidence-dimension-evidence"],
   ["ماتریس عمیق ۱۱۵ × ۷","JSON + CSV","audit-deep-audit-matrix"],
   ["رتبه‌بندی RTPMI 4.2","JSON + CSV","statistics-portal-ranking"],
diff --git a/app/isc-scope/page.tsx b/app/isc-scope/page.tsx
index a7f35e6..4852c55 100644
--- a/app/isc-scope/page.tsx
+++ b/app/isc-scope/page.tsx
@@ -1,3 +1,3 @@
 import {institutions,audits} from "@/lib/data";import Link from "next/link";export const metadata={title:"فهرست دقیق ISC — ۱۱۵ نهاد دولتی"};
-const statusLabel=(s:string)=>s==="direct-official"?"پرتال مستقیم رسمی":s.includes("restricted")?"دسترسی محدود":s==="false-positive-blocked"?"مسیر اشتباه مسدود":s.includes("official")||s.includes("institutional")||s.includes("ministry")?"ارجاع رسمی/سازمانی":"شاهد؛ نیازمند Evidence مستقیم";
+const statusLabel=(s:string)=>s==="direct-official"?"پرتال مستقیم رسمی معاونت پژوهشی و فناوری":s.includes("restricted")?"دسترسی محدود":s==="false-positive-blocked"?"مسیر اشتباه مسدود":s.includes("official")||s.includes("institutional")||s.includes("ministry")?"ارجاع رسمی/سازمانی":"شاهد؛ نیازمند Evidence مستقیم";
 export default function Page(){const cats=["جامع","صنعتی","علوم کشاورزی","هنر","زیرنظام","دستگاه اجرایی"];return <main className="shell page"><header className="pageHero"><span className="kicker">ISC NATIONAL CLASSIFICATION / 1402–1401</span><h1>فهرست قفل‌شده ۱۱۵ نهاد دولتی ISC</h1><p>۶۹ جامع، ۲۴ صنعتی، ۴ علوم کشاورزی، ۴ هنر، ۴ زیرنظام و ۱۰ وابسته به دستگاه‌های اجرایی. رتبه ISC با RTPMI مستقل است.</p><a className="external" href="/datasets/isc-source.json">متادیتای منبع ISC و Reconciliation →</a></header>{cats.map(cat=><section className="scopeGroup" key={cat}><div className="sectionHead"><h2>{cat}</h2><b>{institutions.filter(x=>x.category===cat).length}</b></div><div className="scopeTable">{institutions.filter(x=>x.category===cat).sort((a,b)=>a.iscRank-b.iscRank||a.nameFa.localeCompare(b.nameFa)).map((u:any)=>{const a:any=audits.find(x=>x.universitySlug===u.slug);return <Link href={`/universities/${u.slug}`} className="scopeRow" key={u.slug}><b>{u.iscRank}</b><span>{u.nameFa}</span><i className={`status ${a?.portalAuditStatus}`}>{statusLabel(a?.portalAuditStatus||"")}</i></Link>})}</div></section>)}</main>}
diff --git a/app/layout.tsx b/app/layout.tsx
index 0372b73..51e1b6f 100644
--- a/app/layout.tsx
+++ b/app/layout.tsx
@@ -12,7 +12,7 @@ export const metadata:Metadata={
   metadataBase:new URL(site),
   title:{
     default:"رصدخانه ملی پرتال معاونت پژوهشی و فناوری دانشگاه‌ها",
-    template:"%s | رصدخانه معاونت پژوهشی و فناوری"
+    template:"%s | رصدخانه پرتال معاونت پژوهشی و فناوری"
   },
   description:"مرجع داده‌محور ممیزی پرتال معاونت پژوهشی و فناوری ۱۱۵ دانشگاه و مؤسسه دولتی مطابق ISC؛ شامل RTPMI، شواهد، اسناد و Open Data.",
   alternates:{canonical:"/"},
diff --git a/app/manifest.ts b/app/manifest.ts
index 42d6c73..ac26af9 100644
--- a/app/manifest.ts
+++ b/app/manifest.ts
@@ -1 +1 @@
-import type {MetadataRoute} from "next";export default function manifest():MetadataRoute.Manifest{return{name:"رصد پرتال معاونت پژوهش و فناوری",short_name:"رصد پرتال",description:"ISC-aligned Research & Technology Portal Observatory",start_url:"/",display:"standalone",background_color:"#070913",theme_color:"#070913",lang:"fa",dir:"rtl"}}
+import type {MetadataRoute} from "next";export default function manifest():MetadataRoute.Manifest{return{name:"رصد پرتال معاونت پژوهشی و فناوری",short_name:"پرتال معاونت پژوهشی و فناوری",description:"ISC-aligned Research & Technology Vice-Presidency Portal Observatory",start_url:"/",display:"standalone",background_color:"#070913",theme_color:"#070913",lang:"fa",dir:"rtl"}}
diff --git a/app/methodology/page.tsx b/app/methodology/page.tsx
index 7e5c8f2..fadb863 100644
--- a/app/methodology/page.tsx
+++ b/app/methodology/page.tsx
@@ -1,2 +1,2 @@
-export const metadata={title:"روش‌شناسی RTPMI ۴.۲ | ISC ۱۱۵",description:"روش Evidence-first ممیزی و رتبه‌بندی بلوغ پرتال معاونت پژوهش و فناوری، مستقل از رتبه عملکردی ISC."};
-export default function Page(){return <main className="shell page"><header className="pageHero"><span className="kicker">RTPMI 4.2 / OPEN & REPRODUCIBLE</span><h1>شاخص بلوغ پرتال، نه رتبه عملکرد پژوهشی</h1><p>RTPMI فقط کیفیت و شفافیت سطح عمومی اکوسیستم معاونت پژوهش و فناوری را از Evidence نسخه‌بندی‌شده می‌سنجد.</p></header><article className="glass prose"><h2>Scope دقیق ISC</h2><p>هر ۱۱۵ نهاد دولتی طبقه‌بندی ملی ISC سال ۱۴۰۲–۱۴۰۱ در محصول حضور دارد: ۶۹ جامع، ۲۴ صنعتی، ۴ علوم کشاورزی، ۴ هنر، ۴ زیرنظام و ۱۰ دستگاه اجرایی. نهادهای غیردولتی و علوم پزشکی خارج از Scope هستند.</p><h2>Portal Resolution برای ۱۱۵/۱۱۵</h2><p>هر نهاد یک Outcome دارد: پرتال مستقیم رسمی، ارجاع رسمی/سازمانی، دسترسی محدود، شاهد ثانویه یا false-positive مسدودشده. داشتن Outcome با داشتن پرتال مستقیم یا رتبه عددی یکی نیست.</p><h2>Ranking Eligibility</h2><p>عدد RTPMI فقط وقتی منتشر می‌شود که پرتال <code>direct-official</code> باشد، Audit Evidence Coverage حداقل ۷۵٪ و Confidence حداقل ۶۵٪ باشد. سایر نهادها در National Register باقی می‌مانند ولی Unranked هستند.</p><h2>ابعاد Evidence عمومی</h2><p>مدل عمومی ممیزی شامل ۷ بُعد است: هویت پرتال، ساختار سازمانی، کتابخانه و اسناد، آزمایشگاه‌ها، صنعت و فناوری، سامانه‌ها و خدمات، و اسناد و مقررات. برای ۱۱۵ نهاد در مجموع ۸۰۵ outcome بُعدی ثبت می‌شود.</p><h2>مؤلفه‌ها و وزن‌های RTPMI 4.2</h2><p>اسناد ۲۰٪، ساختار سازمانی ۱۲٪، کتابخانه ۱۰٪، آزمایشگاه ۱۲٪، سامانه‌های پژوهشی ۱۲٪، صنعت و فناوری ۱۲٪، کیفیت داده/Provenance ۱۲٪ و یافت‌پذیری ۱۰٪.</p><h2>Missing Data Rule</h2><p>بُعد <code>unresolved</code> به صفر تبدیل نمی‌شود. آن بُعد از مخرج وزن فعال حذف می‌شود و Audit Coverage/Confidence پایین می‌آید؛ در صورت کافی نبودن Evidence، کل دانشگاه Unranked می‌ماند.</p><h2>شاعا</h2><p>شبکه آزمایشگاه‌های علمی ایران به‌عنوان <code>national-related-system</code> مدل می‌شود، نه آزمایشگاه دانشگاه.</p><h2>ISC Rank ≠ RTPMI</h2><p>رتبه ISC جایگاه مؤسسه در طبقه‌بندی ملی ISC است. RTPMI شاخص اختصاصی این پروژه برای بلوغ پرتال عمومی است و هیچ ادعایی درباره کیفیت علمی/پژوهشی کل دانشگاه ندارد.</p><h2>Reproducibility</h2><p>با اجرای <code>npm run prepare:data</code> ماتریس ۱۱۵×۷، رتبه RTPMI، Provenance Ledger، Open Data، ۱۱۵ Audit Packet و CSVها از داده نسخه‌بندی‌شده دوباره ساخته می‌شوند.</p></article></main>}
+export const metadata={title:"روش‌شناسی RTPMI ۴.۲ | ISC ۱۱۵",description:"روش Evidence-first ممیزی و رتبه‌بندی بلوغ پرتال معاونت پژوهشی و فناوری، مستقل از رتبه عملکردی ISC."};
+export default function Page(){return <main className="shell page"><header className="pageHero"><span className="kicker">RTPMI 4.2 / OPEN & REPRODUCIBLE</span><h1>شاخص بلوغ پرتال معاونت پژوهشی و فناوری، نه رتبه عملکرد پژوهشی دانشگاه</h1><p>RTPMI فقط کیفیت و شفافیت سطح عمومی پرتال معاونت پژوهشی و فناوری را از Evidence نسخه‌بندی‌شده می‌سنجد.</p></header><article className="glass prose"><h2>Scope دقیق ISC</h2><p>هر ۱۱۵ نهاد دولتی طبقه‌بندی ملی ISC سال ۱۴۰۲–۱۴۰۱ در محصول حضور دارد: ۶۹ جامع، ۲۴ صنعتی، ۴ علوم کشاورزی، ۴ هنر، ۴ زیرنظام و ۱۰ دستگاه اجرایی. نهادهای غیردولتی و علوم پزشکی خارج از Scope هستند.</p><h2>Portal Resolution برای ۱۱۵/۱۱۵</h2><p>هر نهاد یک Outcome دارد: پرتال مستقیم رسمی معاونت پژوهشی و فناوری، ارجاع رسمی/سازمانی، دسترسی محدود، شاهد ثانویه یا false-positive مسدودشده. داشتن Outcome با داشتن پرتال مستقیم معاونت پژوهشی و فناوری یا رتبه عددی یکی نیست.</p><h2>Ranking Eligibility</h2><p>عدد RTPMI فقط وقتی منتشر می‌شود که پرتال معاونت پژوهشی و فناوری <code>direct-official</code> باشد، Audit Evidence Coverage حداقل ۷۵٪ و Confidence حداقل ۶۵٪ باشد. سایر نهادها در National Register باقی می‌مانند ولی Unranked هستند.</p><h2>ابعاد Evidence عمومی</h2><p>مدل عمومی ممیزی شامل ۷ بُعد است: هویت پرتال معاونت پژوهشی و فناوری، ساختار سازمانی، کتابخانه و اسناد، آزمایشگاه‌ها، صنعت و فناوری، سامانه‌ها و خدمات، و اسناد و مقررات. برای ۱۱۵ نهاد در مجموع ۸۰۵ outcome بُعدی ثبت می‌شود.</p><h2>مؤلفه‌ها و وزن‌های RTPMI 4.2</h2><p>اسناد ۲۰٪، ساختار سازمانی ۱۲٪، کتابخانه ۱۰٪، آزمایشگاه ۱۲٪، سامانه‌های پژوهشی ۱۲٪، صنعت و فناوری ۱۲٪، کیفیت داده/Provenance ۱۲٪ و یافت‌پذیری ۱۰٪.</p><h2>Missing Data Rule</h2><p>بُعد <code>unresolved</code> به صفر تبدیل نمی‌شود. آن بُعد از مخرج وزن فعال حذف می‌شود و Audit Coverage/Confidence پایین می‌آید؛ در صورت کافی نبودن Evidence، کل دانشگاه Unranked می‌ماند.</p><h2>شاعا</h2><p>شبکه آزمایشگاه‌های علمی ایران به‌عنوان <code>national-related-system</code> مدل می‌شود، نه آزمایشگاه دانشگاه.</p><h2>ISC Rank ≠ RTPMI</h2><p>رتبه ISC جایگاه مؤسسه در طبقه‌بندی ملی ISC است. RTPMI شاخص اختصاصی این پروژه برای بلوغ پرتال عمومی معاونت پژوهشی و فناوری است و هیچ ادعایی درباره کیفیت علمی/پژوهشی کل دانشگاه ندارد.</p><h2>Reproducibility</h2><p>با اجرای <code>npm run prepare:data</code> ماتریس ۱۱۵×۷، رتبه RTPMI، Provenance Ledger، Open Data، ۱۱۵ Audit Packet و CSVها از داده نسخه‌بندی‌شده دوباره ساخته می‌شوند.</p></article></main>}
diff --git a/app/page.tsx b/app/page.tsx
index c239ad8..1cc3ea5 100644
--- a/app/page.tsx
+++ b/app/page.tsx
@@ -13,13 +13,13 @@ export default function Home(){
   return <main>
     <section className="hero shell">
       <div className="heroCopy">
-        <div className="liveBadge"><i/> داده‌نمای ممیزی ۱۱۵ نهاد دولتی ISC <span>نسخه ۱۰.۰</span></div>
-        <h1>پرتال معاونت پژوهشی و فناوری دانشگاه‌ها،<em> زیر ذره‌بین داده.</em></h1>
-        <p>مرجع ملی سنجش بلوغ و شفافیت پرتال معاونت پژوهشی و فناوری دانشگاه‌ها؛ از ساختار معاونت و کتابخانه تا آزمایشگاه، صنعت و فناوری، سامانه‌ها و اسناد.</p>
+        <div className="liveBadge"><i/> ممیزی پرتال معاونت پژوهشی و فناوری ۱۱۵ نهاد دولتی ISC <span>Snapshot ۱۰.۰</span></div>
+        <h1>ردِ شواهد را بگیرید؛<em> پرتال معاونت پژوهشی و فناوری را بسنجید.</em></h1>
+        <p>رصدخانه‌ای برای سنجش بلوغ و شفافیت پرتال معاونت پژوهشی و فناوری دانشگاه‌ها؛ از ساختار معاونت و کتابخانه تا آزمایشگاه، صنعت و فناوری، سامانه‌ها و اسناد.</p>
         <HomeExplorer institutions={institutions} audits={audits} rankings={rankings}/>
         <div className="heroLinks">
-          <Link href="/audit">دیدن نقشه ملی ممیزی <span>←</span></Link>
-          <Link href="/methodology">RTPMI دقیقاً چه می‌سنجد؟</Link>
+          <Link className="primaryLink" href="/audit">باز کردن دفتر ممیزی <span>←</span></Link>
+          <Link className="quietLink" href="/methodology">RTPMI دقیقاً چه می‌سنجد؟</Link>
         </div>
       </div>
       <HeroConstellation audits={researchReviews} summary={summary}/>
@@ -47,10 +47,10 @@ export default function Home(){
       <div className="journeyGrid">
         <Link href="/universities" className="journeyCard violet">
           <span>برای دانشگاه</span>
-          <strong>پرونده پرتال خودتان را باز کنید</strong>
+          <strong>پرونده پرتال معاونت پژوهشی و فناوری خود را باز کنید</strong>
           <p>شکاف‌های شفافیت، وضعیت شواهد، واحدها، سامانه‌ها و اسناد را یک‌جا ببینید.</p>
           <b>پیدا کردن دانشگاه ←</b>
-          <i>01</i>
+          <i>پرونده</i>
         </Link>
 
         <Link href="/audit" className="journeyCard cyan">
@@ -58,7 +58,7 @@ export default function Home(){
           <strong>تصویر ملی ۱۱۵ × ۷ را ببینید</strong>
           <p>پوشش ممیزی را بر اساس گروه ISC و وضعیت دسترسی رصد و مقایسه کنید.</p>
           <b>ورود به نقشه ممیزی ←</b>
-          <i>02</i>
+          <i>سیاست</i>
         </Link>
 
         <Link href="/datasets" className="journeyCard lime">
@@ -66,7 +66,7 @@ export default function Home(){
           <strong>داده را بگیرید و بازتولید کنید</strong>
           <p>JSON، CSV، دفتر شواهد، ۱۱۵ Audit Packet و API نسخه‌دار آماده استفاده‌اند.</p>
           <b>رفتن به Data Lab ←</b>
-          <i>03</i>
+          <i>داده</i>
         </Link>
       </div>
     </section>
@@ -96,7 +96,7 @@ export default function Home(){
           <aside>
             <span>خط قرمز تفسیر</span>
             <h3>RTPMI رتبه علمی دانشگاه نیست.</h3>
-            <p>این شاخص فقط بلوغ، یافت‌پذیری و شفافیت پرتال عمومی معاونت پژوهشی و فناوری را می‌سنجد. پرتال فاقد Evidence کافی به‌جای امتیاز ساختگی، «رتبه‌ناپذیر» می‌ماند.</p>
+            <p>این شاخص فقط بلوغ، یافت‌پذیری و شفافیت پرتال عمومی معاونت پژوهشی و فناوری را می‌سنجد. پرتال معاونت پژوهشی و فناوریِ فاقد Evidence کافی به‌جای امتیاز ساختگی، «رتبه‌ناپذیر» می‌ماند.</p>
             <Link href="/methodology">روش محاسبه و قواعد داده مفقود ←</Link>
           </aside>
         </div>
diff --git a/app/rankings/page.tsx b/app/rankings/page.tsx
index febebfb..d346bf5 100644
--- a/app/rankings/page.tsx
+++ b/app/rankings/page.tsx
@@ -2,5 +2,5 @@ import Link from "next/link";
 import {RankingExplorer} from "@/components/ranking-explorer";
 import {RankPodium} from "@/components/visual-stories";
 import {rankings,datasetSummary} from "@/lib/data";
-export const metadata={title:"رتبه‌بندی RTPMI 4.2 پرتال‌ها"};
-export default function Page(){const s:any=datasetSummary;return <main className="shell page"><header className="pageHero"><div><span className="eyebrow">RTPMI 4.2 · reproducible · evidence qualified</span><h1>رتبه بلوغ پرتال، نه دانشگاه.</h1><p>فقط پرتال رسمی مستقیم با پوشش شواهد حداقل ۷۵٪ و اطمینان حداقل ۶۵٪ رتبه می‌گیرد. ابعاد نامشخص صفر نشده‌اند.</p></div><div className="pageHeroStamp lime"><b>{s.ranked}</b><span>پرتال رتبه‌پذیر</span><small>{s.unranked} نهاد بدون نمره ساختگی</small></div></header><RankPodium rankings={rankings}/><div className="methodBanner"><b>پیش از خواندن جدول:</b><span>RTPMI درباره شفافیت و بلوغ سطح عمومی پرتال است و هیچ گزاره‌ای درباره کیفیت علمی، اعضای هیئت علمی یا خروجی پژوهشی دانشگاه ندارد.</span><Link href="/methodology">جزئیات روش ←</Link></div><RankingExplorer rankings={rankings}/><p className="dataNote">این Snapshot فقط با نسخه RTPMI 4.2 قابل تفسیر است. تغییر روش‌شناسی می‌تواند ترتیب نسخه‌های آینده را تغییر دهد.</p></main>}
+export const metadata={title:"رتبه‌بندی RTPMI 4.2 پرتال معاونت پژوهشی و فناوری"};
+export default function Page(){const s:any=datasetSummary;return <main className="shell page"><header className="pageHero"><div><span className="eyebrow">RTPMI 4.2 · reproducible · evidence qualified</span><h1>رتبه بلوغ پرتال معاونت پژوهشی و فناوری، نه دانشگاه.</h1><p>فقط پرتال رسمی مستقیم معاونت پژوهشی و فناوری با پوشش شواهد حداقل ۷۵٪ و اطمینان حداقل ۶۵٪ رتبه می‌گیرد. ابعاد نامشخص صفر نشده‌اند.</p></div><div className="pageHeroStamp lime"><b>{s.ranked}</b><span>پرتال معاونت پژوهشی و فناوریِ رتبه‌پذیر</span><small>{s.unranked} نهاد بدون نمره ساختگی</small></div></header><RankPodium rankings={rankings}/><div className="methodBanner"><b>پیش از خواندن جدول:</b><span>RTPMI درباره شفافیت و بلوغ سطح عمومی پرتال معاونت پژوهشی و فناوری است و هیچ گزاره‌ای درباره کیفیت علمی، اعضای هیئت علمی یا خروجی پژوهشی دانشگاه ندارد.</span><Link href="/methodology">جزئیات روش ←</Link></div><RankingExplorer rankings={rankings}/><p className="dataNote">این Snapshot فقط با نسخه RTPMI 4.2 قابل تفسیر است. تغییر روش‌شناسی می‌تواند ترتیب نسخه‌های آینده را تغییر دهد.</p></main>}
diff --git a/app/universities/[slug]/page.tsx b/app/universities/[slug]/page.tsx
index 58fd9d6..71fbdea 100644
--- a/app/universities/[slug]/page.tsx
+++ b/app/universities/[slug]/page.tsx
@@ -65,12 +65,12 @@ export async function generateMetadata(
   return {
     title:
       university
-        ? `${university.nameFa} | پرونده پرتال پژوهش`
+        ? `${university.nameFa} | پرونده پرتال معاونت پژوهشی و فناوری`
         : "دانشگاه",
 
     description:
       university
-        ? `پرونده ممیزی پرتال پژوهش و فناوری ${university.nameFa}، وضعیت شواهد، RTPMI، واحدها، سامانه‌ها و اسناد.`
+        ? `پرونده ممیزی پرتال معاونت پژوهشی و فناوری ${university.nameFa}، وضعیت شواهد، RTPMI، واحدها، سامانه‌ها و اسناد.`
         : "",
   };
 }
@@ -81,7 +81,7 @@ const labels:
     string
   > = {
   portalIdentity:
-    "هویت پرتال",
+    "هویت پرتال معاونت پژوهشی و فناوری",
 
   organization:
     "ساختار سازمانی",
@@ -341,9 +341,9 @@ export default async function Page(
           </h1>
 
           <p>
-            نمای یکپارچه از شواهد عمومی معاونت پژوهش و فناوری،
+            نمای یکپارچه از شواهد عمومی پرتال معاونت پژوهشی و فناوری،
             منابع رسمی، واحدها، سامانه‌ها، اسناد و وضعیت بلوغ
-            پرتال. جایگاه ISC و RTPMI دو سنجه مستقل‌اند.
+            این پرتال. جایگاه ISC و RTPMI دو سنجه مستقل‌اند.
           </p>
 
           <div className="profileActions">
@@ -358,17 +358,17 @@ export default async function Page(
                   target="_blank"
                   rel="noopener noreferrer"
                 >
-                  باز کردن پرتال رسمی ↗
+                  باز کردن پرتال رسمی معاونت پژوهشی و فناوری ↗
                 </a>
               )
               : (
                 <span className="disabledAction">
-                  پرتال مستقیم هنوز تأیید نشده
+                  پرتال مستقیم معاونت پژوهشی و فناوری هنوز تأیید نشده
                 </span>
               )}
 
             <ShareButton
-              title={`پرونده پرتال پژوهش ${university.nameFa}`}
+              title={`پرونده پرتال معاونت پژوهشی و فناوری ${university.nameFa}`}
             />
 
             <a
@@ -400,7 +400,7 @@ export default async function Page(
 
                 <div>
                   <small>
-                    رتبه ملی پرتال
+                    رتبه ملی پرتال معاونت پژوهشی و فناوری
                   </small>
 
                   <strong>
@@ -656,7 +656,7 @@ export default async function Page(
               </span>
 
               <h2>
-                پروفایل بلوغ پرتال
+                پروفایل بلوغ پرتال معاونت پژوهشی و فناوری
               </h2>
             </div>
 
diff --git a/app/ux-refresh.css b/app/ux-refresh.css
index b841c7c..54bf01e 100644
--- a/app/ux-refresh.css
+++ b/app/ux-refresh.css
@@ -1,8 +1,25 @@
 :root{
+  --ink:#172036;
+  --muted:#66728a;
+  --line:#d8e0ec;
+  --soft:#eef2f7;
+  --paper:#f4f7fb;
+  --surface:#ffffff;
+  --surface-2:#eef2f7;
+  --night:#111a31;
+  --violet:#3154b8;
+  --violet-soft:#e8edfb;
+  --cyan:#087c70;
+  --cyan-soft:#dff1ed;
+  --lime:#e5a218;
+  --amber:#e5a218;
+  --red:#c95650;
+  --radius:18px;
+  --shadow:0 18px 55px rgba(23,32,54,.08);
   --ux-shadow-sm:0 8px 24px rgba(15,23,42,.055);
   --ux-shadow-md:0 20px 55px rgba(15,23,42,.09);
   --ux-shadow-lg:0 36px 90px rgba(15,23,42,.13);
-  --ux-focus:0 0 0 4px rgba(101,69,232,.13)
+  --ux-focus:0 0 0 4px rgba(49,84,184,.15)
 }
 
 html{
@@ -12,9 +29,10 @@ html{
 body{
   min-height:100vh;
   background:
-    radial-gradient(circle at 85% -10%,rgba(101,69,232,.08),transparent 28rem),
-    radial-gradient(circle at -8% 22%,rgba(0,168,181,.055),transparent 25rem),
+    linear-gradient(rgba(49,84,184,.025) 1px,transparent 1px),
+    linear-gradient(90deg,rgba(49,84,184,.025) 1px,transparent 1px),
     var(--paper);
+  background-size:32px 32px;
   text-rendering:optimizeLegibility
 }
 
@@ -616,3 +634,378 @@ summary:focus-visible{
     max-width:72vw
   }
 }
+
+/* RESEARCH PORTAL IDENTITY */
+
+.brandMark{
+  border-radius:13px 13px 5px 13px;
+  background:var(--night)
+}
+
+.brandMark:before{
+  inset:8px;
+  border:1px solid rgba(255,255,255,.18);
+  border-radius:7px 7px 2px 7px
+}
+
+.brandMark:after{
+  content:"";
+  position:absolute;
+  top:8px;
+  right:8px;
+  bottom:8px;
+  width:2px;
+  border-radius:3px;
+  background:var(--lime)
+}
+
+.brandMark i,
+.brandMark i:nth-child(2),
+.brandMark i:nth-child(3){
+  right:14px;
+  left:auto;
+  width:16px;
+  height:1px;
+  border-radius:2px;
+  background:rgba(255,255,255,.72);
+  box-shadow:none
+}
+
+.brandMark i{top:14px}
+.brandMark i:nth-child(2){top:20px;width:13px}
+.brandMark i:nth-child(3){top:26px;width:9px}
+
+.siteHeader{
+  border-bottom:1px solid color-mix(in srgb,var(--line) 85%,transparent)
+}
+
+.headerCta{
+  border:1px solid var(--night);
+  border-radius:10px 10px 3px 10px;
+  background:var(--night)
+}
+
+.headerCta span{
+  color:var(--lime)
+}
+
+.eyebrow{
+  color:var(--violet);
+  letter-spacing:.55px
+}
+
+.hero{
+  min-height:690px;
+  grid-template-columns:minmax(0,1.03fr) minmax(470px,.97fr);
+  gap:54px;
+  padding-block:54px 66px
+}
+
+.hero h1{
+  max-width:760px;
+  margin:26px 0 20px;
+  font-size:clamp(47px,5.25vw,74px);
+  line-height:1.14;
+  letter-spacing:-3.2px
+}
+
+.hero h1 em{
+  position:relative;
+  color:var(--violet)
+}
+
+.hero h1 em:after{
+  content:"";
+  position:absolute;
+  right:0;
+  bottom:-5px;
+  width:58%;
+  height:4px;
+  border-radius:3px;
+  background:linear-gradient(90deg,var(--lime),var(--red))
+}
+
+.liveBadge{
+  padding:7px 11px;
+  border-radius:10px 10px 3px 10px;
+  border-color:var(--line);
+  box-shadow:0 5px 16px rgba(23,32,54,.04)
+}
+
+.liveBadge i{
+  background:var(--cyan);
+  box-shadow:0 0 0 5px color-mix(in srgb,var(--cyan) 13%,transparent)
+}
+
+.liveBadge span{
+  color:var(--violet)
+}
+
+.heroSearch>label{
+  margin-bottom:9px;
+  color:var(--ink)
+}
+
+.searchBox{
+  height:64px;
+  border-color:color-mix(in srgb,var(--violet) 28%,var(--line));
+  border-radius:14px 14px 4px 14px;
+  box-shadow:0 18px 45px rgba(35,55,104,.1)
+}
+
+.searchBox>span{
+  color:var(--violet)
+}
+
+.searchBox kbd{
+  color:var(--violet);
+  border-color:color-mix(in srgb,var(--violet) 24%,var(--line));
+  background:var(--violet-soft)
+}
+
+.heroLinks{
+  align-items:center;
+  gap:12px;
+  margin-top:20px
+}
+
+.heroLinks a{
+  display:inline-flex;
+  align-items:center;
+  justify-content:center;
+  min-height:42px;
+  padding:0 15px;
+  border-radius:10px;
+  font-size:12px
+}
+
+.heroLinks .primaryLink{
+  color:#fff;
+  background:var(--violet);
+  box-shadow:0 10px 24px rgba(49,84,184,.2)
+}
+
+.heroLinks .primaryLink span{
+  margin-right:8px;
+  color:#fff
+}
+
+.heroLinks .quietLink{
+  border:1px solid var(--line);
+  background:color-mix(in srgb,var(--surface) 74%,transparent)
+}
+
+.proofStrip{
+  border-block-color:var(--line);
+  background:color-mix(in srgb,var(--surface) 90%,transparent)
+}
+
+.proofStrip .shell>div{
+  position:relative
+}
+
+.proofStrip .shell>div:before{
+  content:"";
+  position:absolute;
+  top:0;
+  right:23px;
+  width:22px;
+  height:2px;
+  background:var(--lime)
+}
+
+.proofStrip b{
+  color:var(--night)
+}
+
+.journeyGrid{
+  gap:13px
+}
+
+.journeyCard{
+  min-height:310px;
+  border-radius:18px 18px 5px 18px
+}
+
+.journeyCard>i{
+  left:22px;
+  top:18px;
+  padding:4px 8px;
+  border:1px solid currentColor;
+  border-radius:6px;
+  font-size:8px;
+  font-weight:750;
+  opacity:.48
+}
+
+.journeyCard.violet{
+  border-color:#d6def5;
+  background:#e9eefb
+}
+
+.journeyCard.cyan{
+  border-color:#cbe5df;
+  background:#e3f2ef
+}
+
+.journeyCard.lime{
+  border-color:#f0ddae;
+  background:#fff6df
+}
+
+.darkSection{
+  position:relative;
+  background:
+    linear-gradient(rgba(255,255,255,.025) 1px,transparent 1px),
+    linear-gradient(90deg,rgba(255,255,255,.025) 1px,transparent 1px),
+    var(--night);
+  background-size:32px 32px
+}
+
+.rankingStage aside{
+  border-radius:18px 18px 5px 18px;
+  background:var(--lime);
+  color:#2a210c
+}
+
+.categoryRail a,
+.livingCallout,
+.pageHeroStamp,
+.uniCard,
+.profileScore,
+.dimensionCard,
+.metricCard,
+.catalogCard{
+  border-radius:18px 18px 5px 18px
+}
+
+.livingCallout{
+  border-color:#d6def5;
+  background:#e9eefb
+}
+
+.spectrum,
+.podiumStory{
+  border-radius:22px 22px 7px 22px;
+  background:var(--night)
+}
+
+.spectrumBar .verified,
+.spectrum footer .verified{
+  background:var(--cyan)
+}
+
+.spectrumBar .observed,
+.spectrum footer .observed{
+  background:var(--violet)
+}
+
+.spectrumBar .restricted,
+.spectrum footer .restricted{
+  background:var(--lime)
+}
+
+.spectrumBar .unresolved,
+.spectrum footer .unresolved{
+  background:#364057
+}
+
+[data-theme="dark"]{
+  --ink:#eef2fb;
+  --muted:#a1acc0;
+  --line:#2b3750;
+  --soft:#172136;
+  --paper:#0b1120;
+  --surface:#121b2e;
+  --surface-2:#172136;
+  --night:#080d19;
+  --violet:#829cff;
+  --violet-soft:#202b4d;
+  --cyan:#50c9b8;
+  --cyan-soft:#173d38;
+  --lime:#f1ba45;
+  --amber:#f1ba45;
+  --red:#ee8179;
+  --shadow:0 20px 60px rgba(0,0,0,.34)
+}
+
+[data-theme="dark"] body{
+  background:
+    linear-gradient(rgba(130,156,255,.025) 1px,transparent 1px),
+    linear-gradient(90deg,rgba(130,156,255,.025) 1px,transparent 1px),
+    var(--paper);
+  background-size:32px 32px
+}
+
+[data-theme="dark"] .proofStrip b{
+  color:var(--ink)
+}
+
+[data-theme="dark"] .journeyCard.violet{
+  border-color:#314068;
+  background:#202b4d
+}
+
+[data-theme="dark"] .journeyCard.cyan{
+  border-color:#28534e;
+  background:#173d38
+}
+
+[data-theme="dark"] .journeyCard.lime{
+  border-color:#594923;
+  background:#342b16
+}
+
+[data-theme="dark"] .livingCallout{
+  border-color:#314068;
+  background:#202b4d
+}
+
+@media(max-width:1120px){
+  .hero{
+    grid-template-columns:minmax(0,1fr) minmax(420px,.9fr);
+    gap:35px
+  }
+
+}
+
+@media(max-width:940px){
+  .hero{
+    grid-template-columns:1fr;
+    gap:38px;
+    padding-top:44px
+  }
+
+  .heroCopy{
+    max-width:760px
+  }
+
+}
+
+@media(max-width:620px){
+  .hero{
+    padding-block:34px 48px
+  }
+
+  .hero h1{
+    font-size:clamp(39px,12vw,55px);
+    letter-spacing:-2px
+  }
+
+  .heroLinks{
+    align-items:stretch;
+    flex-direction:column
+  }
+
+}
+
+@media(prefers-reduced-motion:reduce){
+  *,
+  *:before,
+  *:after{
+    scroll-behavior:auto!important;
+    animation-duration:.01ms!important;
+    animation-iteration-count:1!important;
+    transition-duration:.01ms!important
+  }
+}
diff --git a/components/audit-explorer.tsx b/components/audit-explorer.tsx
index 10a61b3..eed01b6 100644
--- a/components/audit-explorer.tsx
+++ b/components/audit-explorer.tsx
@@ -7,7 +7,7 @@ import {
 } from "react";
 
 const dimensions = [
-  ["portalIdentity", "هویت پرتال"],
+  ["portalIdentity", "هویت پرتال معاونت پژوهشی و فناوری"],
   ["organization", "ساختار"],
   ["libraryDocuments", "کتابخانه"],
   ["laboratories", "آزمایشگاه"],
diff --git a/components/compare-explorer.tsx b/components/compare-explorer.tsx
index 625f396..d02eb77 100644
--- a/components/compare-explorer.tsx
+++ b/components/compare-explorer.tsx
@@ -92,7 +92,7 @@ export function CompareExplorer(
       <div className="comparePicker glass">
         <div>
           <span className="eyebrow">
-            انتخاب ۲ تا ۴ پرتال
+            انتخاب ۲ تا ۴ پرتال معاونت پژوهشی و فناوری
           </span>
 
           <h2>
@@ -268,7 +268,7 @@ export function CompareExplorer(
         )}
 
       <p className="dataNote">
-        اعداد فقط برای پرتال‌های Evidence-qualified نمایش داده می‌شوند.
+        اعداد فقط برای پرتال‌های معاونت پژوهشی و فناوریِ Evidence-qualified نمایش داده می‌شوند.
         «نامشخص» در محاسبه صفر نشده است.
       </p>
     </>
diff --git a/components/evidence-explorer.tsx b/components/evidence-explorer.tsx
index bbd729c..8330581 100644
--- a/components/evidence-explorer.tsx
+++ b/components/evidence-explorer.tsx
@@ -4,7 +4,7 @@ import {useMemo,useState} from "react";
 import {PUBLIC_DIMENSION_COUNT,PUBLIC_OUTCOME_COUNT} from "@/lib/public-model";
 
 const dimensionLabels:any={
-  portalIdentity:"هویت پرتال",
+  portalIdentity:"هویت پرتال معاونت پژوهشی و فناوری",
   organization:"ساختار",
   libraryDocuments:"کتابخانه/اسناد",
   laboratories:"آزمایشگاه",
diff --git a/components/header.tsx b/components/header.tsx
index ecc9664..2d5bec7 100644
--- a/components/header.tsx
+++ b/components/header.tsx
@@ -22,7 +22,7 @@ export function Header(){
     <div className="shell headerIn">
       <Link href="/" className="brand" aria-label="صفحه نخست رصدخانه">
         <span className="brandMark" aria-hidden="true"><i/><i/><i/></span>
-        <span><strong>رصدخانه معاونت پژوهشی و فناوری</strong><small>ISC ۱۱۵ · IRAN</small></span>
+        <span><strong>رصدخانه پرتال معاونت پژوهشی و فناوری</strong><small>ISC ۱۱۵ · IRAN</small></span>
       </Link>
       <nav className={open?"nav open":"nav"} aria-label="ناوبری اصلی">
         {nav.map(([href,label])=><Link href={href} key={href} onClick={()=>setOpen(false)} className={pathname.startsWith(href)?"active":""}>{label}</Link>)}
diff --git a/components/ranking-explorer.tsx b/components/ranking-explorer.tsx
index 3730a51..869106c 100644
--- a/components/ranking-explorer.tsx
+++ b/components/ranking-explorer.tsx
@@ -3,4 +3,4 @@
 import Link from "next/link";
 import {useMemo, useState} from "react";
 
-export function RankingExplorer({rankings}:{rankings:any[]}){const [category,setCategory]=useState("همه");const [query,setQuery]=useState("");const rows=useMemo(()=>rankings.filter(r=>(category==="همه"||r.iscCategory===category)&&(!query||r.nameFa.includes(query))),[rankings,category,query]);return <><div className="explorerBar compact glass"><div className="field searchField"><label htmlFor="ranking-search">جست‌وجو</label><input id="ranking-search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="نام دانشگاه…"/></div><div className="field"><label htmlFor="ranking-category">گروه ISC</label><select id="ranking-category" value={category} onChange={e=>setCategory(e.target.value)}><option>همه</option>{[...new Set(rankings.map(x=>x.iscCategory))].map(x=><option key={x}>{x}</option>)}</select></div></div><div className="rankTable"><div className="rankHead"><span>رتبه</span><span>پرتال دانشگاه</span><span>در گروه ISC</span><span>پوشش / اطمینان</span><span>RTPMI</span></div>{rows.map((r:any)=><Link href={`/universities/${r.universitySlug}`} className="rankRow" key={r.universitySlug}><b>{r.rank.toLocaleString("fa-IR")}</b><span><strong>{r.nameFa}</strong><small>{r.iscCategory} · رتبه ISC {r.iscRank.toLocaleString("fa-IR")}</small></span><i>{r.portalRankWithinISCClass.toLocaleString("fa-IR")} از {r.rankedPortalsInISCClass.toLocaleString("fa-IR")}</i><div className="confidence"><span><i style={{width:`${r.confidence}%`}}/></span><small>{r.evidenceCoverage}% پوشش · {r.confidence}% اطمینان</small></div><strong>{r.score}</strong></Link>)}</div></>}
+export function RankingExplorer({rankings}:{rankings:any[]}){const [category,setCategory]=useState("همه");const [query,setQuery]=useState("");const rows=useMemo(()=>rankings.filter(r=>(category==="همه"||r.iscCategory===category)&&(!query||r.nameFa.includes(query))),[rankings,category,query]);return <><div className="explorerBar compact glass"><div className="field searchField"><label htmlFor="ranking-search">جست‌وجو</label><input id="ranking-search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="نام دانشگاه…"/></div><div className="field"><label htmlFor="ranking-category">گروه ISC</label><select id="ranking-category" value={category} onChange={e=>setCategory(e.target.value)}><option>همه</option>{[...new Set(rankings.map(x=>x.iscCategory))].map(x=><option key={x}>{x}</option>)}</select></div></div><div className="rankTable"><div className="rankHead"><span>رتبه</span><span>پرتال معاونت پژوهشی و فناوری</span><span>در گروه ISC</span><span>پوشش / اطمینان</span><span>RTPMI</span></div>{rows.map((r:any)=><Link href={`/universities/${r.universitySlug}`} className="rankRow" key={r.universitySlug}><b>{r.rank.toLocaleString("fa-IR")}</b><span><strong>{r.nameFa}</strong><small>{r.iscCategory} · رتبه ISC {r.iscRank.toLocaleString("fa-IR")}</small></span><i>{r.portalRankWithinISCClass.toLocaleString("fa-IR")} از {r.rankedPortalsInISCClass.toLocaleString("fa-IR")}</i><div className="confidence"><span><i style={{width:`${r.confidence}%`}}/></span><small>{r.evidenceCoverage}% پوشش · {r.confidence}% اطمینان</small></div><strong>{r.score}</strong></Link>)}</div></>}
diff --git a/components/university-explorer.tsx b/components/university-explorer.tsx
index 1f7be90..931b552 100644
--- a/components/university-explorer.tsx
+++ b/components/university-explorer.tsx
@@ -4,7 +4,7 @@ import Link from "next/link";
 import {useMemo,useState} from "react";
 
 type Props={institutions:any[];audits:any[];rankings:any[];documents:any[];reviews:any[]};
-const statusLabel=(s:string)=>s==="direct-official"?"پرتال مستقیم":s.includes("restricted")?"دسترسی محدود":s==="false-positive-blocked"?"مسیر مسدود":"نیازمند شواهد بیشتر";
+const statusLabel=(s:string)=>s==="direct-official"?"پرتال مستقیم معاونت پژوهشی و فناوری":s.includes("restricted")?"دسترسی محدود":s==="false-positive-blocked"?"مسیر مسدود":"نیازمند شواهد بیشتر";
 
 export function UniversityExplorer({institutions,audits,rankings,documents,reviews}:Props){
   const [query,setQuery]=useState("");
@@ -28,7 +28,7 @@ export function UniversityExplorer({institutions,audits,rankings,documents,revie
     return a.category.localeCompare(b.category,"fa")||a.iscRank-b.iscRank;
   }),[institutions,query,category,status,sort,audit,rank,coverage,documentCount]);
   return <>
-    <div className="explorerBar glass"><div className="field searchField"><label htmlFor="directory-search">جست‌وجو</label><input id="directory-search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="نام دانشگاه…"/></div><div className="field"><label htmlFor="category-filter">گروه ISC</label><select id="category-filter" value={category} onChange={e=>setCategory(e.target.value)}><option>همه</option>{["جامع","صنعتی","علوم کشاورزی","هنر","زیرنظام","دستگاه اجرایی"].map(x=><option key={x}>{x}</option>)}</select></div><div className="field"><label htmlFor="status-filter">وضعیت</label><select id="status-filter" value={status} onChange={e=>setStatus(e.target.value)}><option value="همه">همه وضعیت‌ها</option><option value="ranked">دارای RTPMI</option><option value="direct">پرتال مستقیم</option><option value="pending">نیازمند تکمیل شواهد</option></select></div><div className="field"><label htmlFor="sort-filter">مرتب‌سازی</label><select id="sort-filter" value={sort} onChange={e=>setSort(e.target.value)}><option value="isc">گروه و رتبه ISC</option><option value="evidence">بیشترین پوشش شواهد</option><option value="documents">بیشترین سند</option><option value="portal">پرتال مستقیم</option><option value="rtpmi">امتیاز RTPMI</option><option value="name-asc">نام: الف تا ی</option><option value="name-desc">نام: ی تا الف</option></select></div></div>
+    <div className="explorerBar glass"><div className="field searchField"><label htmlFor="directory-search">جست‌وجو</label><input id="directory-search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="نام دانشگاه…"/></div><div className="field"><label htmlFor="category-filter">گروه ISC</label><select id="category-filter" value={category} onChange={e=>setCategory(e.target.value)}><option>همه</option>{["جامع","صنعتی","علوم کشاورزی","هنر","زیرنظام","دستگاه اجرایی"].map(x=><option key={x}>{x}</option>)}</select></div><div className="field"><label htmlFor="status-filter">وضعیت</label><select id="status-filter" value={status} onChange={e=>setStatus(e.target.value)}><option value="همه">همه وضعیت‌ها</option><option value="ranked">دارای RTPMI</option><option value="direct">پرتال مستقیم معاونت پژوهشی و فناوری</option><option value="pending">نیازمند تکمیل شواهد</option></select></div><div className="field"><label htmlFor="sort-filter">مرتب‌سازی</label><select id="sort-filter" value={sort} onChange={e=>setSort(e.target.value)}><option value="isc">گروه و رتبه ISC</option><option value="evidence">بیشترین پوشش شواهد</option><option value="documents">بیشترین سند</option><option value="portal">پرتال مستقیم معاونت پژوهشی و فناوری</option><option value="rtpmi">امتیاز RTPMI</option><option value="name-asc">نام: الف تا ی</option><option value="name-desc">نام: ی تا الف</option></select></div></div>
     <div className="resultMeta"><b>{rows.length.toLocaleString("fa-IR")}</b> دانشگاه از ۱۱۵ <span>·</span> هیچ دانشگاهی به علت کمبود شواهد حذف نشده است.</div>
     <div className="uniGrid">{rows.map(u=>{const a:any=audit.get(u.slug);const r:any=rank.get(u.slug);const evidence=coverage.get(u.slug)??0;const docs=documentCount.get(u.slug)??0;return <Link href={`/universities/${u.slug}`} className="uniCard" key={u.slug}><div className="uniCardTop"><span className="categoryPill">{u.category}</span><span className={`evidencePill ${a?.portalAuditStatus==="direct-official"?"verified":"pending"}`}>{statusLabel(a?.portalAuditStatus||"")}</span></div><h2>{u.nameFa}</h2><p>رتبه {u.iscRank.toLocaleString("fa-IR")} در گروه ISC</p><div className="uniEvidenceMeta"><span><b>{Number(evidence).toLocaleString("fa-IR")}%</b> پوشش شواهد</span><span><b>{docs.toLocaleString("fa-IR")}</b> سند طبقه‌بندی‌شده</span></div><div className="uniScore">{r?<><span><b>{Number(r.score).toLocaleString("fa-IR")}</b><small>RTPMI</small></span><span><b>{Number(r.confidence).toLocaleString("fa-IR")}%</b><small>اطمینان</small></span></>:<span className="unranked"><b>—</b><small>رتبه منتشر نشده</small></span>}<i>مشاهده پرونده ←</i></div></Link>})}</div>
     {!rows.length&&<div className="emptyState"><b>نتیجه‌ای با این ترکیب پیدا نشد.</b><button onClick={()=>{setQuery("");setCategory("همه");setStatus("همه")}}>پاک کردن فیلترها</button></div>}
diff --git a/components/visual-stories.tsx b/components/visual-stories.tsx
index d7e5f76..92acd34 100644
--- a/components/visual-stories.tsx
+++ b/components/visual-stories.tsx
@@ -2,7 +2,7 @@ import Link from "next/link";
 import {formatFaNumber} from "@/lib/fa";
 
 const dimensionLabels:any={
-  portalIdentity:"هویت پرتال",
+  portalIdentity:"هویت پرتال معاونت پژوهشی و فناوری",
   organization:"ساختار",
   libraryDocuments:"کتابخانه",
   laboratories:"آزمایشگاه",
@@ -16,7 +16,7 @@ export function HeroConstellation({audits,summary}:{audits:any[];summary:any}){
 
   return <div className="constellation" aria-label={`رادار ارزیابی ${formatFaNumber(institutionCount)} دانشگاه و مؤسسه`}>
     <div className="auroraBlob violet"/><div className="auroraBlob cyan"/><div className="signalSweep"/>
-    <div className="constellationHeader"><span><i/> رادار ارزیابی ملی</span><b>{formatFaNumber(institutionCount)} نهاد · ۷ بُعد</b></div>
+    <div className="constellationHeader"><span><i/> رادار ملی پرتال معاونت پژوهشی و فناوری</span><b>{formatFaNumber(institutionCount)} نهاد · ۷ بُعد</b></div>
     <div className="constellationField">
       {audits.map((row,index)=>{
         const coverage=row.reviewEvidenceCoverage??row.auditEvidenceCoverage??0;
@@ -34,13 +34,13 @@ export function HeroConstellation({audits,summary}:{audits:any[];summary:any}){
       <div className="constellationCore">
         <span>دامنه ارزیابی</span>
         <b>{formatFaNumber(institutionCount)}</b>
-        <small>دانشگاه و مؤسسه ISC</small>
+        <small>پرتال معاونت پژوهشی و فناوری نهادهای ISC</small>
         <em>{formatFaNumber(summary.ranked)} رتبه‌پذیر</em>
         <i className="corePulse"/>
       </div>
       <div className="orbitLine one"/><div className="orbitLine two"/><div className="orbitLine three"/>
       <div className="constellationSignals">
-        <span><b>{formatFaNumber(summary.directOfficialPortals||0)}</b>پرتال مستقیم</span>
+        <span><b>{formatFaNumber(summary.directOfficialPortals||0)}</b>پرتال مستقیم معاونت پژوهشی و فناوری</span>
         <span><b>{formatFaNumber(summary.documents||0)}</b>سند/شاخص</span>
         <span><b>{formatFaNumber(summary.ranked||0)}</b>رتبه‌پذیر</span>
       </div>
@@ -116,8 +116,8 @@ export function RankPodium({rankings}:{rankings:any[]}){
 
   return <section className="podiumStory">
     <header>
-      <div><span className="eyebrow">RTPMI portal podium</span><h2>سه پرتال پیشرو در این Snapshot</h2></div>
-      <p>ارتفاع ستون از امتیاز بلوغ پرتال می‌آید؛ اندازه دانشگاه یا کیفیت علمی را نمایش نمی‌دهد.</p>
+      <div><span className="eyebrow">RTPMI portal podium</span><h2>سه پرتال معاونت پژوهشی و فناوری پیشرو در این Snapshot</h2></div>
+      <p>ارتفاع ستون از امتیاز بلوغ پرتال معاونت پژوهشی و فناوری می‌آید؛ اندازه دانشگاه یا کیفیت علمی را نمایش نمی‌دهد.</p>
     </header>
     <div className="podiumStage">
       <div className="podiumGrid"/>
