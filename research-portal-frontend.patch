diff --git a/app/page.tsx b/app/page.tsx
index c239ad8..0c56b2b 100644
--- a/app/page.tsx
+++ b/app/page.tsx
@@ -1,6 +1,6 @@
 import Link from "next/link";
 import {HomeExplorer} from "@/components/home-explorer";
-import {HeroConstellation} from "@/components/visual-stories";
+import {EvidenceLedger} from "@/components/visual-stories";
 import {audits,datasetSummary,institutions,rankings,researchReviews} from "@/lib/data";
 
 const fa=(n:number)=>n.toLocaleString("fa-IR");
@@ -13,16 +13,16 @@ export default function Home(){
   return <main>
     <section className="hero shell">
       <div className="heroCopy">
-        <div className="liveBadge"><i/> داده‌نمای ممیزی ۱۱۵ نهاد دولتی ISC <span>نسخه ۱۰.۰</span></div>
-        <h1>پرتال معاونت پژوهشی و فناوری دانشگاه‌ها،<em> زیر ذره‌بین داده.</em></h1>
-        <p>مرجع ملی سنجش بلوغ و شفافیت پرتال معاونت پژوهشی و فناوری دانشگاه‌ها؛ از ساختار معاونت و کتابخانه تا آزمایشگاه، صنعت و فناوری، سامانه‌ها و اسناد.</p>
+        <div className="liveBadge"><i/> دفتر شواهد ۱۱۵ نهاد دولتی ISC <span>Snapshot ۱۰.۰</span></div>
+        <h1>ردِ شواهد را بگیرید؛<em> پرتال پژوهشی را بسنجید.</em></h1>
+        <p>رصدخانه‌ای برای سنجش بلوغ و شفافیت پرتال معاونت پژوهشی و فناوری دانشگاه‌ها؛ از ساختار معاونت و کتابخانه تا آزمایشگاه، صنعت و فناوری، سامانه‌ها و اسناد.</p>
         <HomeExplorer institutions={institutions} audits={audits} rankings={rankings}/>
         <div className="heroLinks">
-          <Link href="/audit">دیدن نقشه ملی ممیزی <span>←</span></Link>
-          <Link href="/methodology">RTPMI دقیقاً چه می‌سنجد؟</Link>
+          <Link className="primaryLink" href="/audit">باز کردن دفتر ممیزی <span>←</span></Link>
+          <Link className="quietLink" href="/methodology">RTPMI دقیقاً چه می‌سنجد؟</Link>
         </div>
       </div>
-      <HeroConstellation audits={researchReviews} summary={summary}/>
+      <EvidenceLedger audits={researchReviews} summary={summary}/>
     </section>
 
     <section className="proofStrip">
@@ -50,7 +50,7 @@ export default function Home(){
           <strong>پرونده پرتال خودتان را باز کنید</strong>
           <p>شکاف‌های شفافیت، وضعیت شواهد، واحدها، سامانه‌ها و اسناد را یک‌جا ببینید.</p>
           <b>پیدا کردن دانشگاه ←</b>
-          <i>01</i>
+          <i>پرونده</i>
         </Link>
 
         <Link href="/audit" className="journeyCard cyan">
@@ -58,7 +58,7 @@ export default function Home(){
           <strong>تصویر ملی ۱۱۵ × ۷ را ببینید</strong>
           <p>پوشش ممیزی را بر اساس گروه ISC و وضعیت دسترسی رصد و مقایسه کنید.</p>
           <b>ورود به نقشه ممیزی ←</b>
-          <i>02</i>
+          <i>سیاست</i>
         </Link>
 
         <Link href="/datasets" className="journeyCard lime">
@@ -66,7 +66,7 @@ export default function Home(){
           <strong>داده را بگیرید و بازتولید کنید</strong>
           <p>JSON، CSV، دفتر شواهد، ۱۱۵ Audit Packet و API نسخه‌دار آماده استفاده‌اند.</p>
           <b>رفتن به Data Lab ←</b>
-          <i>03</i>
+          <i>داده</i>
         </Link>
       </div>
     </section>
diff --git a/app/ux-refresh.css b/app/ux-refresh.css
index b841c7c..1c606d7 100644
--- a/app/ux-refresh.css
+++ b/app/ux-refresh.css
@@ -1,8 +1,25 @@
 :root{
+  --ink:#172036;
+  --muted:#66728a;
+  --line:#d8e0ec;
+  --soft:#eef2f7;
+  --paper:#f4f7fb;
+  --surface:#ffffff;
+  --surface-2:#eef2f7;
+  --night:#111a31;
+  --violet:#3154b8;
+  --violet-soft:#e8edfb;
+  --cyan:#087c70;
+  --cyan-soft:#dff1ed;
+  --lime:#e5a218;
+  --amber:#e5a218;
+  --red:#c95650;
+  --radius:18px;
+  --shadow:0 18px 55px rgba(23,32,54,.08);
   --ux-shadow-sm:0 8px 24px rgba(15,23,42,.055);
   --ux-shadow-md:0 20px 55px rgba(15,23,42,.09);
   --ux-shadow-lg:0 36px 90px rgba(15,23,42,.13);
-  --ux-focus:0 0 0 4px rgba(101,69,232,.13)
+  --ux-focus:0 0 0 4px rgba(49,84,184,.15)
 }
 
 html{
@@ -12,9 +29,10 @@ html{
 body{
   min-height:100vh;
   background:
-    radial-gradient(circle at 85% -10%,rgba(101,69,232,.08),transparent 28rem),
-    radial-gradient(circle at -8% 22%,rgba(0,168,181,.055),transparent 25rem),
+    linear-gradient(rgba(49,84,184,.025) 1px,transparent 1px),
+    linear-gradient(90deg,rgba(49,84,184,.025) 1px,transparent 1px),
     var(--paper);
+  background-size:32px 32px;
   text-rendering:optimizeLegibility
 }
 
@@ -616,3 +634,791 @@ summary:focus-visible{
     max-width:72vw
   }
 }
+
+/* RESEARCH LEDGER IDENTITY */
+
+.srOnly{
+  position:absolute;
+  width:1px;
+  height:1px;
+  padding:0;
+  margin:-1px;
+  overflow:hidden;
+  clip:rect(0,0,0,0);
+  white-space:nowrap;
+  border:0
+}
+
+.brandMark{
+  border-radius:13px 13px 5px 13px;
+  background:var(--night)
+}
+
+.brandMark:before{
+  inset:8px;
+  border:1px solid rgba(255,255,255,.18);
+  border-radius:7px 7px 2px 7px
+}
+
+.brandMark:after{
+  content:"";
+  position:absolute;
+  top:8px;
+  right:8px;
+  bottom:8px;
+  width:2px;
+  border-radius:3px;
+  background:var(--lime)
+}
+
+.brandMark i,
+.brandMark i:nth-child(2),
+.brandMark i:nth-child(3){
+  right:14px;
+  left:auto;
+  width:16px;
+  height:1px;
+  border-radius:2px;
+  background:rgba(255,255,255,.72);
+  box-shadow:none
+}
+
+.brandMark i{top:14px}
+.brandMark i:nth-child(2){top:20px;width:13px}
+.brandMark i:nth-child(3){top:26px;width:9px}
+
+.siteHeader{
+  border-bottom:1px solid color-mix(in srgb,var(--line) 85%,transparent)
+}
+
+.headerCta{
+  border:1px solid var(--night);
+  border-radius:10px 10px 3px 10px;
+  background:var(--night)
+}
+
+.headerCta span{
+  color:var(--lime)
+}
+
+.eyebrow{
+  color:var(--violet);
+  letter-spacing:.55px
+}
+
+.hero{
+  min-height:690px;
+  grid-template-columns:minmax(0,1.03fr) minmax(470px,.97fr);
+  gap:54px;
+  padding-block:54px 66px
+}
+
+.hero h1{
+  max-width:760px;
+  margin:26px 0 20px;
+  font-size:clamp(47px,5.25vw,74px);
+  line-height:1.14;
+  letter-spacing:-3.2px
+}
+
+.hero h1 em{
+  position:relative;
+  color:var(--violet)
+}
+
+.hero h1 em:after{
+  content:"";
+  position:absolute;
+  right:0;
+  bottom:-5px;
+  width:58%;
+  height:4px;
+  border-radius:3px;
+  background:linear-gradient(90deg,var(--lime),var(--red))
+}
+
+.liveBadge{
+  padding:7px 11px;
+  border-radius:10px 10px 3px 10px;
+  border-color:var(--line);
+  box-shadow:0 5px 16px rgba(23,32,54,.04)
+}
+
+.liveBadge i{
+  background:var(--cyan);
+  box-shadow:0 0 0 5px color-mix(in srgb,var(--cyan) 13%,transparent)
+}
+
+.liveBadge span{
+  color:var(--violet)
+}
+
+.heroSearch>label{
+  margin-bottom:9px;
+  color:var(--ink)
+}
+
+.searchBox{
+  height:64px;
+  border-color:color-mix(in srgb,var(--violet) 28%,var(--line));
+  border-radius:14px 14px 4px 14px;
+  box-shadow:0 18px 45px rgba(35,55,104,.1)
+}
+
+.searchBox>span{
+  color:var(--violet)
+}
+
+.searchBox kbd{
+  color:var(--violet);
+  border-color:color-mix(in srgb,var(--violet) 24%,var(--line));
+  background:var(--violet-soft)
+}
+
+.heroLinks{
+  align-items:center;
+  gap:12px;
+  margin-top:20px
+}
+
+.heroLinks a{
+  display:inline-flex;
+  align-items:center;
+  justify-content:center;
+  min-height:42px;
+  padding:0 15px;
+  border-radius:10px;
+  font-size:12px
+}
+
+.heroLinks .primaryLink{
+  color:#fff;
+  background:var(--violet);
+  box-shadow:0 10px 24px rgba(49,84,184,.2)
+}
+
+.heroLinks .primaryLink span{
+  margin-right:8px;
+  color:#fff
+}
+
+.heroLinks .quietLink{
+  border:1px solid var(--line);
+  background:color-mix(in srgb,var(--surface) 74%,transparent)
+}
+
+.evidenceLedger{
+  position:relative;
+  min-width:0;
+  min-height:565px;
+  padding:22px;
+  overflow:hidden;
+  color:var(--ink);
+  border:1px solid color-mix(in srgb,var(--violet) 22%,var(--line));
+  border-right:4px solid var(--violet);
+  border-radius:26px 26px 8px 26px;
+  background:
+    linear-gradient(90deg,transparent 41px,color-mix(in srgb,var(--red) 22%,transparent) 42px,transparent 43px),
+    repeating-linear-gradient(0deg,transparent 0 31px,color-mix(in srgb,var(--line) 42%,transparent) 32px),
+    var(--surface);
+  box-shadow:0 34px 85px rgba(30,45,82,.16)
+}
+
+.evidenceLedger:before{
+  content:"";
+  position:absolute;
+  top:0;
+  right:0;
+  width:34%;
+  height:5px;
+  background:linear-gradient(90deg,var(--cyan),var(--lime),var(--red))
+}
+
+.ledgerHeader,
+.ledgerHeader>div,
+.ledgerStatus,
+.ledgerLegend{
+  display:flex;
+  align-items:center
+}
+
+.ledgerHeader{
+  position:relative;
+  z-index:1;
+  justify-content:space-between;
+  gap:14px;
+  padding-bottom:17px;
+  border-bottom:1px solid var(--line)
+}
+
+.ledgerHeader>div{
+  gap:10px
+}
+
+.ledgerHeader>div>span:last-child{
+  display:grid
+}
+
+.ledgerHeader b{
+  font-size:12px
+}
+
+.ledgerHeader small{
+  color:var(--muted);
+  direction:ltr;
+  font-size:7px;
+  letter-spacing:1.1px
+}
+
+.ledgerSeal{
+  width:34px;
+  height:34px;
+  display:grid;
+  place-items:center;
+  color:#fff;
+  border-radius:9px 9px 3px 9px;
+  background:var(--violet);
+  font-size:16px;
+  font-weight:850;
+  box-shadow:inset 0 0 0 4px rgba(255,255,255,.13)
+}
+
+.ledgerStatus{
+  gap:7px;
+  color:var(--cyan);
+  font-size:8px;
+  font-weight:750
+}
+
+.ledgerStatus i{
+  width:6px;
+  height:6px;
+  border-radius:50%;
+  background:var(--cyan);
+  box-shadow:0 0 0 4px color-mix(in srgb,var(--cyan) 12%,transparent)
+}
+
+.ledgerSummary{
+  display:grid;
+  grid-template-columns:1fr 1.35fr;
+  gap:18px;
+  padding:21px 0 18px
+}
+
+.ledgerSummary>div{
+  display:grid;
+  align-content:start
+}
+
+.ledgerSummary>div>span,
+.ledgerSummary>div>small{
+  color:var(--muted);
+  font-size:8px
+}
+
+.ledgerSummary>div>b{
+  font-size:52px;
+  line-height:1.05;
+  letter-spacing:-2px
+}
+
+.ledgerSummary dl{
+  display:grid;
+  grid-template-columns:repeat(3,1fr);
+  align-items:stretch;
+  margin:0;
+  border:1px solid var(--line);
+  border-radius:14px 14px 4px 14px;
+  background:color-mix(in srgb,var(--surface) 78%,var(--soft))
+}
+
+.ledgerSummary dl>div{
+  display:flex;
+  flex-direction:column;
+  justify-content:center;
+  min-width:0;
+  padding:10px;
+  border-left:1px solid var(--line)
+}
+
+.ledgerSummary dl>div:last-child{
+  border-left:0
+}
+
+.ledgerSummary dt{
+  overflow:hidden;
+  color:var(--muted);
+  font-size:7px;
+  text-overflow:ellipsis;
+  white-space:nowrap
+}
+
+.ledgerSummary dd{
+  margin:2px 0 0;
+  font-size:18px;
+  font-weight:800
+}
+
+.ledgerTable{
+  border:1px solid var(--line);
+  border-radius:15px 15px 5px 15px;
+  overflow:hidden;
+  background:color-mix(in srgb,var(--surface) 88%,transparent)
+}
+
+.ledgerColumns{
+  display:grid;
+  grid-template-columns:minmax(0,1fr) 210px 44px;
+  gap:10px;
+  align-items:end;
+  min-height:48px;
+  padding:8px 12px 7px;
+  color:var(--muted);
+  background:var(--soft);
+  font-size:7px
+}
+
+.ledgerColumns>div{
+  display:grid;
+  grid-template-columns:repeat(7,1fr);
+  align-items:end;
+  gap:4px
+}
+
+.ledgerColumns i{
+  overflow:hidden;
+  font-style:normal;
+  text-align:center;
+  text-overflow:clip;
+  writing-mode:vertical-rl;
+  white-space:nowrap
+}
+
+.ledgerColumns>b{
+  font-size:7px;
+  text-align:left
+}
+
+.ledgerRow{
+  display:grid;
+  grid-template-columns:24px minmax(0,1fr) 210px 44px;
+  gap:9px;
+  align-items:center;
+  min-height:51px;
+  padding:8px 12px;
+  border-top:1px solid var(--line);
+  text-decoration:none;
+  transition:background-color .16s,transform .16s
+}
+
+.ledgerRow:hover{
+  background:var(--violet-soft);
+  transform:translateX(-2px)
+}
+
+.ledgerIndex{
+  color:var(--muted);
+  direction:ltr;
+  font-size:7px
+}
+
+.ledgerName{
+  display:grid;
+  min-width:0
+}
+
+.ledgerName b{
+  overflow:hidden;
+  font-size:9px;
+  text-overflow:ellipsis;
+  white-space:nowrap
+}
+
+.ledgerName small{
+  color:var(--muted);
+  font-size:7px
+}
+
+.ledgerTrace{
+  position:relative;
+  display:grid;
+  grid-template-columns:repeat(7,1fr);
+  gap:4px;
+  align-items:center
+}
+
+.ledgerTrace:before{
+  content:"";
+  position:absolute;
+  right:5%;
+  left:5%;
+  top:50%;
+  height:1px;
+  background:var(--line)
+}
+
+.traceCell{
+  position:relative;
+  z-index:1;
+  width:12px;
+  height:12px;
+  margin:auto;
+  border:2px solid var(--surface);
+  border-radius:50%;
+  box-shadow:0 0 0 1px color-mix(in srgb,var(--line) 90%,transparent)
+}
+
+.traceCell.verified,
+.ledgerLegend i.verified{
+  background:var(--cyan)
+}
+
+.traceCell.observed-reference,
+.ledgerLegend i.observed-reference{
+  background:var(--violet)
+}
+
+.traceCell.restricted,
+.ledgerLegend i.restricted{
+  background:var(--lime)
+}
+
+.traceCell.unresolved,
+.ledgerLegend i.unresolved{
+  background:#aeb7c7
+}
+
+.ledgerRow>strong{
+  color:var(--ink);
+  direction:ltr;
+  font-size:15px;
+  text-align:left
+}
+
+.ledgerRow>strong small{
+  color:var(--muted);
+  font-size:7px
+}
+
+.ledgerLegend{
+  gap:12px;
+  flex-wrap:wrap;
+  padding-top:16px;
+  color:var(--muted);
+  font-size:7px
+}
+
+.ledgerLegend span{
+  display:flex;
+  align-items:center;
+  gap:4px
+}
+
+.ledgerLegend i{
+  width:7px;
+  height:7px;
+  border-radius:50%
+}
+
+.ledgerLegend a{
+  margin-right:auto;
+  color:var(--violet);
+  font-size:8px;
+  font-weight:800;
+  text-decoration:none
+}
+
+.proofStrip{
+  border-block-color:var(--line);
+  background:color-mix(in srgb,var(--surface) 90%,transparent)
+}
+
+.proofStrip .shell>div{
+  position:relative
+}
+
+.proofStrip .shell>div:before{
+  content:"";
+  position:absolute;
+  top:0;
+  right:23px;
+  width:22px;
+  height:2px;
+  background:var(--lime)
+}
+
+.proofStrip b{
+  color:var(--night)
+}
+
+.journeyGrid{
+  gap:13px
+}
+
+.journeyCard{
+  min-height:310px;
+  border-radius:18px 18px 5px 18px
+}
+
+.journeyCard>i{
+  left:22px;
+  top:18px;
+  padding:4px 8px;
+  border:1px solid currentColor;
+  border-radius:6px;
+  font-size:8px;
+  font-weight:750;
+  opacity:.48
+}
+
+.journeyCard.violet{
+  border-color:#d6def5;
+  background:#e9eefb
+}
+
+.journeyCard.cyan{
+  border-color:#cbe5df;
+  background:#e3f2ef
+}
+
+.journeyCard.lime{
+  border-color:#f0ddae;
+  background:#fff6df
+}
+
+.darkSection{
+  position:relative;
+  background:
+    linear-gradient(rgba(255,255,255,.025) 1px,transparent 1px),
+    linear-gradient(90deg,rgba(255,255,255,.025) 1px,transparent 1px),
+    var(--night);
+  background-size:32px 32px
+}
+
+.rankingStage aside{
+  border-radius:18px 18px 5px 18px;
+  background:var(--lime);
+  color:#2a210c
+}
+
+.categoryRail a,
+.livingCallout,
+.pageHeroStamp,
+.uniCard,
+.profileScore,
+.dimensionCard,
+.metricCard,
+.catalogCard{
+  border-radius:18px 18px 5px 18px
+}
+
+.livingCallout{
+  border-color:#d6def5;
+  background:#e9eefb
+}
+
+.spectrum,
+.podiumStory{
+  border-radius:22px 22px 7px 22px;
+  background:var(--night)
+}
+
+.spectrumBar .verified,
+.spectrum footer .verified{
+  background:var(--cyan)
+}
+
+.spectrumBar .observed,
+.spectrum footer .observed{
+  background:var(--violet)
+}
+
+.spectrumBar .restricted,
+.spectrum footer .restricted{
+  background:var(--lime)
+}
+
+.spectrumBar .unresolved,
+.spectrum footer .unresolved{
+  background:#364057
+}
+
+[data-theme="dark"]{
+  --ink:#eef2fb;
+  --muted:#a1acc0;
+  --line:#2b3750;
+  --soft:#172136;
+  --paper:#0b1120;
+  --surface:#121b2e;
+  --surface-2:#172136;
+  --night:#080d19;
+  --violet:#829cff;
+  --violet-soft:#202b4d;
+  --cyan:#50c9b8;
+  --cyan-soft:#173d38;
+  --lime:#f1ba45;
+  --amber:#f1ba45;
+  --red:#ee8179;
+  --shadow:0 20px 60px rgba(0,0,0,.34)
+}
+
+[data-theme="dark"] body{
+  background:
+    linear-gradient(rgba(130,156,255,.025) 1px,transparent 1px),
+    linear-gradient(90deg,rgba(130,156,255,.025) 1px,transparent 1px),
+    var(--paper);
+  background-size:32px 32px
+}
+
+[data-theme="dark"] .evidenceLedger{
+  background:
+    linear-gradient(90deg,transparent 41px,rgba(238,129,121,.18) 42px,transparent 43px),
+    repeating-linear-gradient(0deg,transparent 0 31px,rgba(130,156,255,.055) 32px),
+    var(--surface)
+}
+
+[data-theme="dark"] .ledgerSummary dl,
+[data-theme="dark"] .ledgerTable{
+  background:rgba(18,27,46,.9)
+}
+
+[data-theme="dark"] .proofStrip b{
+  color:var(--ink)
+}
+
+[data-theme="dark"] .journeyCard.violet{
+  border-color:#314068;
+  background:#202b4d
+}
+
+[data-theme="dark"] .journeyCard.cyan{
+  border-color:#28534e;
+  background:#173d38
+}
+
+[data-theme="dark"] .journeyCard.lime{
+  border-color:#594923;
+  background:#342b16
+}
+
+[data-theme="dark"] .livingCallout{
+  border-color:#314068;
+  background:#202b4d
+}
+
+@media(max-width:1120px){
+  .hero{
+    grid-template-columns:minmax(0,1fr) minmax(420px,.9fr);
+    gap:35px
+  }
+
+  .ledgerColumns{
+    grid-template-columns:minmax(0,1fr) 180px 40px
+  }
+
+  .ledgerRow{
+    grid-template-columns:20px minmax(0,1fr) 180px 40px
+  }
+}
+
+@media(max-width:940px){
+  .hero{
+    grid-template-columns:1fr;
+    gap:38px;
+    padding-top:44px
+  }
+
+  .heroCopy{
+    max-width:760px
+  }
+
+  .evidenceLedger{
+    width:min(100%,680px);
+    margin-inline:auto
+  }
+}
+
+@media(max-width:620px){
+  .hero{
+    padding-block:34px 48px
+  }
+
+  .hero h1{
+    font-size:clamp(39px,12vw,55px);
+    letter-spacing:-2px
+  }
+
+  .heroLinks{
+    align-items:stretch;
+    flex-direction:column
+  }
+
+  .evidenceLedger{
+    min-height:0;
+    padding:16px;
+    border-radius:20px 20px 6px 20px
+  }
+
+  .ledgerStatus{
+    display:none
+  }
+
+  .ledgerSummary{
+    grid-template-columns:1fr
+  }
+
+  .ledgerSummary>div{
+    grid-template-columns:auto 1fr;
+    align-items:end
+  }
+
+  .ledgerSummary>div>b{
+    grid-row:1/3;
+    margin-left:12px;
+    font-size:46px
+  }
+
+  .ledgerColumns{
+    grid-template-columns:minmax(0,1fr) 132px 35px
+  }
+
+  .ledgerColumns i{
+    font-size:0
+  }
+
+  .ledgerColumns i:after{
+    content:"•";
+    font-size:8px
+  }
+
+  .ledgerRow{
+    grid-template-columns:16px minmax(0,1fr) 132px 35px;
+    gap:5px;
+    padding-inline:8px
+  }
+
+  .traceCell{
+    width:10px;
+    height:10px
+  }
+
+  .ledgerLegend{
+    gap:8px
+  }
+
+  .ledgerLegend a{
+    width:100%;
+    margin-top:4px
+  }
+}
+
+@media(prefers-reduced-motion:reduce){
+  *,
+  *:before,
+  *:after{
+    scroll-behavior:auto!important;
+    animation-duration:.01ms!important;
+    animation-iteration-count:1!important;
+    transition-duration:.01ms!important
+  }
+}
diff --git a/components/visual-stories.tsx b/components/visual-stories.tsx
index d7e5f76..435411c 100644
--- a/components/visual-stories.tsx
+++ b/components/visual-stories.tsx
@@ -55,6 +55,70 @@ export function HeroConstellation({audits,summary}:{audits:any[];summary:any}){
   </div>;
 }
 
+const ledgerDimensions=[
+  ["portalIdentity","هویت"],
+  ["organization","ساختار"],
+  ["libraryDocuments","کتابخانه"],
+  ["laboratories","آزمایشگاه"],
+  ["industryTechnology","صنعت"],
+  ["systemsServices","سامانه"],
+  ["documentsRegulations","اسناد"],
+] as const;
+
+const outcomeLabel:Record<string,string>={
+  verified:"تأیید مستقیم",
+  "observed-reference":"شاهد یا ارجاع",
+  restricted:"دسترسی محدود",
+  unresolved:"حل‌نشده",
+};
+
+export function EvidenceLedger({audits,summary}:{audits:any[];summary:any}){
+  const sample=audits.slice(0,5);
+
+  return <div className="evidenceLedger" aria-label={`نمونه دفتر شواهد ${formatFaNumber(audits.length)} نهاد`}>
+    <header className="ledgerHeader">
+      <div><span className="ledgerSeal" aria-hidden="true">ر</span><span><b>دفتر ممیزی ملی</b><small>EVIDENCE LEDGER / SNAPSHOT 10.0</small></span></div>
+      <span className="ledgerStatus"><i/> داده منتشرشده</span>
+    </header>
+
+    <div className="ledgerSummary">
+      <div><span>دامنه ثبت</span><b>{formatFaNumber(audits.length||115)}</b><small>دانشگاه و مؤسسه ISC</small></div>
+      <dl>
+        <div><dt>پرتال مستقیم</dt><dd>{formatFaNumber(summary.directOfficialPortals||0)}</dd></div>
+        <div><dt>ردیف شواهد</dt><dd>{formatFaNumber(summary.provenanceRecords||0)}</dd></div>
+        <div><dt>رتبه‌پذیر</dt><dd>{formatFaNumber(summary.ranked||0)}</dd></div>
+      </dl>
+    </div>
+
+    <div className="ledgerTable">
+      <div className="ledgerColumns" aria-hidden="true">
+        <span>نهاد</span>
+        <div>{ledgerDimensions.map(([,label])=><i key={label}>{label}</i>)}</div>
+        <b>پوشش</b>
+      </div>
+      {sample.map((row,index)=><Link href={`/universities/${row.universitySlug}`} className="ledgerRow" key={row.universitySlug}>
+        <span className="ledgerIndex">{formatFaNumber(index+1,{minimumIntegerDigits:2})}</span>
+        <span className="ledgerName"><b>{row.nameFa}</b><small>{row.iscCategory}</small></span>
+        <span className="ledgerTrace">
+          {ledgerDimensions.map(([key,label])=>{
+            const outcome=row.dimensions?.[key]||"unresolved";
+            return <i className={`traceCell ${outcome}`} title={`${label}: ${outcomeLabel[outcome]||outcome}`} key={key}><span className="srOnly">{label}: {outcomeLabel[outcome]||outcome}</span></i>;
+          })}
+        </span>
+        <strong>{formatFaNumber(row.reviewEvidenceCoverage||0)}<small>٪</small></strong>
+      </Link>)}
+    </div>
+
+    <footer className="ledgerLegend">
+      <span><i className="verified"/> تأیید مستقیم</span>
+      <span><i className="observed-reference"/> شاهد/ارجاع</span>
+      <span><i className="restricted"/> محدود</span>
+      <span><i className="unresolved"/> حل‌نشده</span>
+      <Link href="/evidence">همه شواهد ←</Link>
+    </footer>
+  </div>;
+}
+
 export function EvidenceSpectrum({dimensions}:{dimensions:Record<string,Record<string,number>>}){
   const entries=Object.entries(dimensions).filter(([key])=>Object.prototype.hasOwnProperty.call(dimensionLabels,key));
   const values=entries.map(([,value])=>value);
