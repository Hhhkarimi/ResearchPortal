# مشارکت در رصدخانه

هر تغییر داده باید قابل بازبینی و بازتولید باشد. هدف افزایش پوشش Evidence است، نه پر کردن ظاهری خانه‌های خالی.

## تغییر داده یک دانشگاه

1. Slug دانشگاه را فقط از `data/isc/institutions.json` انتخاب کنید.
2. URL منبع رسمی و تاریخ مشاهده را ثبت کنید.
3. میان «واحد تابع»، «سامانه مرتبط» و «ارجاع عمومی» تمایز بگذارید.
4. برای IT فقط با شاهد رسمی رابطه سازمانی، وضعیت `verified` ثبت کنید.
5. `unresolved` را بدون شاهد مستقیم به `verified` تغییر ندهید.
6. بعد از تغییر، `npm run release:check` را اجرا کنید.

## تغییر روش RTPMI

تغییر وزن، Gate یا رفتار داده مفقود باید:

- نسخه روش‌شناسی جدید داشته باشد؛
- در `docs/RTPMI_4_1_METHODOLOGY.md` یا سند نسخه بعد ثبت شود؛
- با Snapshot قبل به‌عنوان رتبه قابل مقایسه معرفی نشود؛
- خروجی‌های رتبه‌بندی و مستندات را هم‌زمان بازتولید کند.

## Pull Request

- مسئله و دامنه تغییر را کوتاه توضیح دهید.
- URL و نوع Evidence را ذکر کنید.
- خروجی گیت‌های validation، typecheck، lint و build را بنویسید.
- محتوای HTML یا فایل منبع خارجی را بدون مجوز داخل Repository کپی نکنید.
