# گزارش Performance Engineering

مبنای اندازه‌گیری: build تولیدی Next.js 16.3 با Webpack، قبل و بعد از بهینه‌سازی. اعداد پاسخ شامل HTML و RSC جاسازی‌شده هستند؛ ستون gzip تقریب نزدیک به payload شبکه است.

## نتیجه عددی

| مسیر | قبل (خام) | بعد (خام) | کاهش | قبل (gzip) | بعد (gzip) | کاهش gzip |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `/` | 1,434,010 B | 134,161 B | 90.6٪ | 154,968 B | 23,371 B | 84.9٪ |
| `/universities` | 12,388,250 B | 125,402 B | 99.0٪ | 1,029,792 B | 15,054 B | 98.5٪ |
| `/documents` | 11,647,452 B | 188,987 B | 98.4٪ | 940,192 B | 23,109 B | 97.5٪ |
| `/evidence` | 7,950,577 B | 382,199 B | 95.2٪ | 611,478 B | 36,766 B | 94.0٪ |
| `/audit` | 3,657,873 B | 164,236 B | 95.5٪ | 285,971 B | 16,555 B | 94.2٪ |

- زمان تولید استاتیک ۲۵۴ مسیر: از ۱۴.۵ ثانیه به ۷.۶ ثانیه؛ ۴۷.۶٪ سریع‌تر.
- بزرگ‌ترین chunk اختصاصی صفحه: حدود ۵.۶KB؛ افزایش bundle سنگین ایجاد نشده است.
- API جدید اسناد به‌صورت استاتیک تولید می‌شود و ۴۷۹۱ رکورد نمایه را فقط هنگام تعامل کاربر تحویل می‌دهد.

## اصلاحات مهندسی

- کاهش RSC serialization با DTOهای کم‌حجم و مخصوص هر Client Component
- تبدیل پنج prop سنگین صفحه دانشگاه‌ها به یک آرایه ۱۱۵‌ردیفی آماده نمایش
- حذف آرایه‌های منابع و URLهای تو‌در‌تو از payload صفحات ممیزی و شواهد
- رندر اولیه فقط ۹۶ سند و بارگذاری نمایه کامل هنگام focus، filter یا Load more
- استفاده از `useDeferredValue` برای جلوگیری از کندشدن تایپ هنگام جست‌وجو
- memoization برای Mapها و گزینه‌های فیلتر، و استفاده از lookupهای O(1)
- ساخت indexهای immutable در سطح module برای API و ۱۱۵ صفحه دانشگاه
- ادغام چند traversal مستقل شواهد در یک loop
- انتقال بومی‌سازی سراسری ارقام به `requestIdleCallback` و batch کردن MutationObserver
- استفاده از `content-visibility:auto` برای فهرست‌های خارج viewport
- استفاده از functional state update برای منوی موبایل

## حفظ رفتار

- نمودار راداری صفحه اول حفظ شده است.
- نام «پرتال معاونت پژوهشی و فناوری» در رابط حفظ شده است.
- جست‌وجو و فیلتر همه ۴۷۹۱ سند همچنان در دسترس است؛ فقط انتقال نمایه کامل از مسیر critical rendering خارج شده است.

## اعتبارسنجی

- `npm run typecheck` — موفق
- `npm run lint` — بدون خطا؛ همان ۷ هشدار قدیمی و نامرتبط
- production build — موفق؛ ۲۵۴ مسیر
- production smoke test — موفق
- API نمایه اسناد — ۴۷۹۱/۴۷۹۱ رکورد
- `git diff --check` و `git apply --check` — موفق
